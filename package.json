{
  "name": "@agora-type/contracts",
  "version": "0.0.1",
  "description": "AgoRace - Weekly typing competition with on-chain prize pool",
  "scripts": {
    "build": "forge build",
    "clean": "forge clean",
    "format:check": "forge fmt --check src",
    "format": "forge fmt src",
    "generate-interfaces": "./scripts/generate-interfaces",
    "test": "FOUNDRY_PROFILE=test forge test",
    "test:v": "FOUNDRY_PROFILE=test forge test -vvv"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2",
    "@openzeppelin/contracts-upgradeable": "^5.0.2"
  },
  "devDependencies": {
    "ds-test": "github:dapphub/ds-test",
    "forge-std": "github:foundry-rs/forge-std"
  }
}
